import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'los-peines-de-ro-desktop',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
