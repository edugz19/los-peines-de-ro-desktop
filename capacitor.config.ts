import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Los Peines de Ro',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
